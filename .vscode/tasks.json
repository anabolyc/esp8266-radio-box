{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start debug web-server",
            "type": "shell",
            "command": "docker rm -f lighttpd-instance ; docker run --rm -ti --name lighttpd-instance -v ${PWD}/data:/var/www/localhost -p 8080:8080 arulrajnet/lighttpd:latest",
            "problemMatcher": []
        }
    ]
}